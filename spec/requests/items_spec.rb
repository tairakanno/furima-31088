require 'rails_helper'

RSpec.describe ItemsController, type: :request do
  # before do
  #   @item = FactoryBot.create(:item)
  # end
  # describe "GET #index" do
  #   it "indexアクションにリクエストすると正常にレスポンスが返ってくる" do
  #     get root_path
  #     expect(response.status).to eq 200
  #   end
  #   it "indexアクションにリクエストするとレスポンスに投稿済みのitemのnameが存在する" do
  #     get root_path
  #     expect(response.body).to include @item.name
  #   end
  #   it "indexアクションにリクエストするとレスポンスに投稿済みのitemの画像URLが存在する" do
  #     get root_path
  #     expect(response.body).to include @item.image
  #   end
  #   it "indexアクションにリクエストするとレスポンスに投稿済みのitemのpriceが存在する" do
  #     get root_path
  #     expect(response.body).to include @item.price
  #   end
  # end
end
